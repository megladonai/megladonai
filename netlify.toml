[build]
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# If you already have a netlify.toml at your project root with a
# [build] publish = "..." line, just add the "functions" line
# inside that same [build] block instead of creating a second one.